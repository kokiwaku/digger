PORT=8787
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB_NAME=digger
FRONTEND_ORIGIN=http://localhost:5173

# LLM_PROVIDER=mock (デフォルト、LLMを呼ばない) または vertex (Vertex AI Geminiを使用)
LLM_PROVIDER=mock

# LLM_PROVIDER=vertex のときに必要。詳細は backend/README.md の
# 「Vertex AI (Gemini) のセットアップ」を参照。
GCP_PROJECT_ID=
GCP_LOCATION=
GEMINI_MODEL=
